<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisdom.interaction.mapper.CommentMapper">

    <select id="add">
        insert comment(com_id, info_id, user_id, com_content, role) values (#{comId}, #{infoId}, #{userId},  #{comContent}, #{role})
    </select>

</mapper>
